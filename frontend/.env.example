# API base URL — the backend FastAPI service
# In Docker Compose this should be http://backend:8000
# For local dev (npm run dev) keep http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
